# Copy to frontend/.env.local for local overrides, or set in Vercel's
# Project Settings -> Environment Variables for production builds.
#
# Must point at your deployed Railway backend + the /api/v1 prefix.
# Example: https://cinematch-backend-production.up.railway.app/api/v1
VITE_API_BASE_URL=
